## 1. Ordering helpers

- [x] 1.1 Ввести функцию канонической сортировки имён контролов (стабильная `localeCompare`, единый контракт для тестов).
- [x] 1.2 Упорядочить массив lighting composites по детерминированному ключу (имя switch / `compositeId`).
- [x] 1.3 После сбора `mappableControls` заменить порядок обхода `Map` на канонический список (исключая thermostat/lighting consumed).

## 2. WirenboardDevice.create

- [x] 2.1 Применить канонический порядок в шагах, где итерируются контролы для обычных WB-устройств (не трогать `system`/`system__*` сверх минимально необходимого или явно исключить по договорённости).
- [x] 2.2 Убедиться, что `buildDeviceGrouping` / `buildControlGrouping` получают списки в каноническом порядке; теги `tag: 1..n` соответствуют этому порядку.
- [x] 2.3 При необходимости по design: добавить явные `EndpointNumber` в опции `MatterbridgeEndpoint` / дочерних частей и проверить отсутствие коллизий. _(Пока не требуется: стабильность через канонический порядок + комментарий в `canonicalOrdering.ts`; явные номера — follow-up.)_

## 3. Платформа и регистрация (опционально в этом change)

- [x] 3.1 Оценить сортировку `deviceMap` по имени устройства в `registerDiscoveredDevices` для стабильности между WB-устройствами; реализовать или вынести в follow-up.

## 4. Тесты и документация

- [x] 4.1 Unit-тесты: один и тот же набор контролов в разном порядке `Map` даёт одинаковый порядок endpoint’ов / идентификаторов частей.
- [x] 4.2 При наличии явных номеров — тест на отсутствие дубликатов в пределах сценария. _(Явных номеров нет; проверка уникальности id в тесте control mode.)_
- [x] 4.3 `AGENTS.md` или README: кратко описать канонический порядок и возможную однократную перенумерацию после обновления.
- [x] 4.4 Тесты: semantic tags при обратном порядке контролов в `Map`; последовательные `create` с разным порядком `Map`; порядок `registerDevice` для двух устройств; `design.md` — live MQTT (`verify:mqtt`) и границы юнит-тестов vs Matter.js.
